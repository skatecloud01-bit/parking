// OAuth callback routes are no longer needed — authentication is handled by Supabase Auth.
// This file is kept as a placeholder to avoid import errors during the transition.
import type { Express } from "express";
export function registerOAuthRoutes(_app: Express) {}
